services:
  web:
    build: .
    container_name: fsa-website
    ports:
      - "8087:80"
    restart: unless-stopped
    env_file: /home/debian/.env
    networks:
      - cloudflare

networks:
  cloudflare:
    external: true
